<%= form_for([@project, @project.proposals.build]) do |f| %>
	<%= render "shared/error_messages", object: @project %>
	<p>
		<%= f.label :content %><br/>
		<%= f.text_area :content %>
	</p>
	<%= f.submit %>
<% end %>
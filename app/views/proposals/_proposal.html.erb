<p>
	<strong>Content:</strong>
	<%= proposal.reason %>
	<%= proposal.passions %>
	<%= proposal.skills %>
	<%= proposal.work %>
	<%= proposal.commitment %>
	<%= proposal.estimation %>
	<%= proposal.linkedin %>
  	<%= link_to proposal.project_id, project_path(:id => proposal.project_id) %>
</p>